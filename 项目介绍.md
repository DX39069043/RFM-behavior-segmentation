# 用户价值与购买潜力分层（UserValue&Potential）——项目全流程详解

以下按"给零基础读者"的方式，从**项目要解决什么问题**讲起，逐步走完整个流水线。全程会标注"为什么这么做"，并给出本项目实际跑出来的数字。

---

## 一、这个项目在解决什么问题

电商平台每天有海量行为日志（谁看了什么、谁加了购物车、谁买了什么），但**"谁该被运营触达"**是个难题：

- 传统 RFM 模型只看**已购买**用户的最近购买、频次、金额——对**没买过的用户完全失效**（频次 0、金额 0）；
- 而平台最想抓的恰恰是两类人：**还没买、但很有希望首购的人**，以及**买了很多、但可能流失的高价值用户**。

本项目用 **2019 年 10 月的用户行为**给每个用户打上"人群标签 + 购买概率分"，再用**独立的 11 月数据**检验这些标签是否真的能预测"下个月会不会买"，并在 7 个月面板上做跨月稳健性检验与标签稳定性分析。

---

## 二、数据长什么样

数据来自 Kaggle 电商行为数据集（e-Commerce Behavior Data from Multi Category Store）。本项目统一使用**按 user_id 哈希抽样 5% 的 7 个月用户面板**（约 78 万用户 / 2060 万条事件）：

| 字段 | 含义 |
|---|---|
| `event_time` | 事件时间（UTC） |
| `event_type` | 事件类型：**view**（浏览）/ **cart**（加购）/ **purchase**（购买） |
| `product_id` / `category_code` / `brand` | 商品信息 |
| `price` | 价格 |
| `user_id` | 用户 |
| `user_session` | 会话 ID |

时间上分成两段（这是项目的方法论核心）：

| 时间段 | 事件数 | 用途 |
|---|---:|---|
| 2019-10 | 2,117,423（15.1 万用户） | **建模**：构建特征、拟合阈值、打人群标签 |
| 2019-11 | 3,391,302 | **验证**：检验 10 月打的标签能否预测 11 月购买 |

> **为什么拆两段？** 这是"时间外验证"（out-of-time validation）：所有模型参数只用 10 月拟合，11 月数据从头到尾不参与建模，只在最后当"考卷"。这样能防止**信息泄露**——比如用"11 月买了"去倒推 10 月应该给谁打高分，那分数就毫无意义了。

---

## 三、项目文件结构

```
RFM-behavior-segmentation/
├── main.ipynb          ← 唯一入口：数据分析全流程（EDA → 分层 → 滚动验证 → 选人 → 队列迁移）
├── analysis.py         ← 函数库：所有分析逻辑（特征、分层、检验、基准、打分、队列迁移）
├── config.py           ← 集中配置：路径、月份、随机种子、会话阈值等
├── run_rolling.py      ← 滚动验证入口（7 个月面板）
├── run_cohort.py       ← 队列迁移入口（7 个月面板）
├── sample_user_cohort.py  ← 按用户哈希抽样生成 7 个月面板
├── tests/              ← 单元测试（22 个用例）
└── 产物：
    ├── tracked_users_list_Nov.csv  ← 6,792 名候选运营名单（10 列）
    ├── rolling_validation_results.csv / rolling_baseline_results.csv
    └── cohort_frozen_labels.csv
```

设计原则：**Notebook 只管"加载数据 + 画图 + 调用"，所有计算逻辑都在 `analysis.py` 里**，保证可复现、可测试。

---

## 四、流水线逐步骤详解（10 月建模）

### 步骤 1：特征工程（把事件日志变成"每个用户一行"）

对 10 月事件，按 `user_id + user_session` 排序，计算每个用户的原始特征：

| 特征 | 怎么算 | 意义 |
|---|---|---|
| `Pages_Viewed` | 浏览事件数 | 浏览深度 |
| `Estimated_Time` | 会话内相邻事件间隔 < 30 分钟的累计时长 | 有效停留时长（避免跨会话空档被算进浏览时间） |
| `Session_Count` | 不同会话数 | 回访频率 |
| `Cart_Products` | 加购事件的**去重商品数** | 购物车意图 |
| `Purchased_Products` | 购买事件的去重商品数 | 购买覆盖 |
| `Purchase_Frequency` | 购买事件数 | 购买频次 |
| `Total_Spending` | 购买金额之和 | 消费贡献 |
| `Recency_Days` | 观察期末距最近一次购买的天数 | 购买新近度 |

### 步骤 2：构造可解释指标

在原始特征之上，构建 2 类指标：

**① 探索度 `E_Score`**（衡量用户多"主动"）
```
E_Score = 平均( Z(log1p(浏览页数)), Z(log1p(停留时长)), Z(log1p(会话数)) )
```
`log1p(x)=ln(1+x)` 压缩长尾，`Z` 是标准化，三者**等权平均**。

**② 决策摩擦 `Friction`**（未购人群：衡量"买得有多费劲"）
```
Friction = max(去重加购商品数 − 去重购买商品数, 0)，取 log1p
```
"加购了却没买"——首购意图受阻的直接证据，与浏览页数无关。

**③ 价值指数 `Value_Index`**（已购用户的 RFM 连续化评分）
```
Value_Index = log1p(消费额) + log1p(购买频次) − log1p(最近购买天数)
```

### 步骤 3：用户分层（`segment_users`）——先分流，再各自切分

**为什么不直接对所有用户做 K-Means？** 因为 85% 的用户没买过，他们频次/金额全是 0，聚类会退化成一堆"零购买簇"、标签失去意义。所以采用**两阶段分层**：

```
               10月用户 (151,447)
                     │
        ┌────是否购买过────┐
        │ 未购 (133,927)   │ 已购 (17,520)
        ▼                 ▼
   E_Score 与 Friction     价值指数上四分位（前 25% 分界）(VIP 4,381)
   的 GMM 阈值              │
        │              ┌───┴───┐
  高潜力首购 5,408    深度互动 2,018  直购 2,363
  普通浏览 128,519
```

关键机制——**GMM 阈值（`gmm_intersection_threshold`）**：对某个指标拟合**双成分高斯混合模型**（GMM，假设人群由"低分群/高分群"两个正态分布混合而成），取两成分**后验概率相等**的位置作为切分阈值。实测阈值：未购 E_Score = −0.066、未购 log(Friction) ≈ 0（92% 未购用户无加购 → 退化为"**有加购即高摩擦**"）、价值指数 = 5.211、VIP E_Score = 1.318。

**"高价值高摩擦用户"（跨月沉默）**：基期高价值买家在观察月完全无任何行为（view/cart/purchase 都没有），由 `flag_buyer_silence` 标记为流失判定。

### 步骤 4：时间外验证（滚动，正式验证）

**实验一：高潜力首购用户真的更可能首购吗？**（2019-10→11 组）

| 组别 | 人数 | 11 月购买率 | 95% CI |
|---|---:|---:|---|
| 高潜力首购用户 | 5,408 | **13.30%** | [12.42%, 14.23%] |
| 普通浏览用户 | 128,519 | **5.41%** | [5.29%, 5.54%] |

购买率差 **+7.88 个百分点**，卡方检验 p = 2.2×10⁻¹³¹（极显著）。且该结论在 7 个月面板上**跨月稳定**（6/6 个月对显著，购买率差 +4.5~+9.3pp）。

**实验二：跨月沉默的高价值用户是否在流失？**（3 月组滚动验证：基期 t 高价值 → 观察月 t+1 完全沉默 → 验证月 t+2 是否购买）

5/5 个月组极显著，沉默组购买率（5.9%~9.6%）远低于活跃高价值对照（25.8%~41.4%），购买率差 **−19.9~−31.8pp**（p<1e-68）——"跨月完全沉默"是可靠流失信号。

> ⚠️ 边界声明：这是**观察性时间外预测验证**，证明"标签能预测行为"，**不等于**"营销能带来增量"——真正的因果结论需要随机 A/B 实验。

---

## 五、逻辑回归基准（回答"规则到底有多好"）

分层规则是"手工阈值 + 二值标签"，它**解释性强但排序能力弱**。项目用逻辑回归（5 折交叉验证，评估用折外 OOF 预测）对比：

| 指标（未购人群 2019-10→11 组） | 规则分层 | 逻辑回归 |
|---|---:|---:|
| AUC（排序能力） | 0.528 | **0.673** |
| 同规模 Top-5408 首购率 | 13.30% | **16.31%** |

**结论：规则是"保守的可解释代理"，个体排序能力弱于直接建模；同预算下 LR 选人首购率从 13.3% 提到 16.3%。**

---

## 六、概率分 Top-k 选人（`score_nonbuyers` / `score_buyers`）

分层规则保留作"解释层"（怎么触达、讲什么话术），**实际选人改用概率分排序（排序层）**：

- 未购用户 → `First_Purchase_Prob`（首购概率分）+ `First_Purchase_Rank` + `TopK_Flag`（同预算下应触达的前 k 人）
- 已购用户 → `Repurchase_Prob`（复购概率分）+ `Repurchase_Rank`（Top-k 复购运营 / Bottom-k 流失预警）

名单 `tracked_users_list_Nov.csv`（6,792 人 = 5,408 高潜力首购 + 1,384 沉默高价值，10 列）。运营用法：按预算取 `rank ≤ 预算` 即可选人。

---

## 七、队列迁移分析：标签是保持还是转化？

固定 2019-10 为基期，用**冻结的 10 月阈值**逐月重算同一批用户的标签（阈值冻结保证跨月可比，不受每月重拟合的阈值漂移污染；E_Score 的 Z 标准化参数同样在基期拟合后冻结，避免"尺子"每月漂移），回答"高价值直购 / 深度互动 / 常规已购 等标签在后续月份是保持还是转化"。

核心发现：
- **标签是"月度快照"而非持久属性**：已购三标签次月保持率仅 10.9%~18.8%（直购最低），6 个月后降至 1.8%~6.1%——运营名单需**按月刷新**；
- **"无任何活动"（当月完全无行为）是主去向**：所有标签占比 6 个月后升至 69%~78%；
- **高价值深度互动粘性最强**：次月购买占比最高（46.0%）、无任何活动占比最低（24%）→ 适合长期会员运营；
- **常规已购流失风险最高**：40.5% 次月无任何活动 → 需及时唤醒。

---

## 八、如何运行

1. `pip install -r requirements.txt`（版本已锁定，含 pyarrow）；
2. 准备 7 个月面板 `data/panel_7months.parquet`（`python sample_user_cohort.py --input-dir <按月csv目录> --out data/panel_7months.parquet`）；
3. 打开 `main.ipynb` 从上到下运行：数据加载（只读 10/11 月，约 40 秒）→ EDA → 分层 → 滚动验证 → 汇总 → LR 基准 → 概率分选人 → 名单导出 → 队列迁移分析；
4. `python -m unittest discover -s tests` 跑 22 个单元测试。

---

## 九、一张图总结全流程

```
7个月面板(78万用户) → 10月建模(15.1万用户) → 特征工程+指标构建(E_Score/Friction/Value_Index)
   → GMM 阈值两阶段分层（未购/已购分流）
   → 滚动时间外验证（实验一 +7.88pp / 实验二 −19.9~−31.8pp，跨月稳定）
   → LR 基准（AUC 0.528→0.673）+ 概率分 Top-k 选人
   → tracked_users_list_Nov.csv（6,792 人）→ 随机 A/B 实验抽样框
   → 队列迁移分析（标签保持率 12%~17% → 2%~6%，按月刷新名单）
```

**一句话版本**：用 10 月行为把用户分成"未购（看首购潜力）"和"已购（看价值与复购）"两翼，各自用可解释指标 + GMM 阈值打标签，用 7 个月面板做滚动时间外验证证明标签有效，再用逻辑回归证明"直接建模排序更强"，最终输出带概率分的运营名单，并追踪标签的跨月稳定性。
