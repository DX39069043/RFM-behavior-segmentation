# 用户价值与购买潜力分层（UserValue&Potential）——项目全流程详解

以下按"给零基础读者"的方式，从**项目要解决什么问题**讲起，逐步走完整个流水线。全程会标注"为什么这么做"，并给出本项目实际跑出来的数字。

---

## 一、这个项目在解决什么问题

电商平台每天有海量行为日志（谁看了什么、谁加了购物车、谁买了什么），但**"谁该被运营触达"**是个难题：

- 传统 RFM 模型只看**已购买**用户的最近购买、频次、金额——对**没买过的用户完全失效**（频次 0、金额 0）；
- 而平台最想抓的恰恰是两类人：**还没买、但很有希望首购的人**，以及**买了很多、但可能流失的高价值用户**。

本项目用 **2019 年 10 月的行为日志**给每个用户打上"人群标签 + 购买概率分"，再用**独立的 11 月数据**检验这些标签是否真的能预测"下个月会不会买"。

---

## 二、数据长什么样

数据来自 Kaggle 电商行为数据集（e-Commerce Behavior Data from Multi Category Store），本项目抽样共 **568,227 条事件日志**，覆盖 10,000 个用户：

| 字段                                     | 含义                                                         |
| ---------------------------------------- | ------------------------------------------------------------ |
| `event_time`                             | 事件时间（UTC）                                              |
| `event_type`                             | 事件类型：**view**（浏览）/ **cart**（加购）/ **purchase**（购买） |
| `product_id` / `category_code` / `brand` | 商品信息                                                     |
| `price`                                  | 价格                                                         |
| `user_id`                                | 用户                                                         |
| `user_session`                           | 会话 ID                                                      |

时间上分成两段（这是项目的方法论核心）：

| 时间段 | 事件数                                             | 用途                                           |
| ------ | -------------------------------------------------- | ---------------------------------------------- |
| 10 月  | 308,850（浏览 296,323 / 加购 6,350 / 购买 6,177）  | **建模**：构建特征、拟合阈值、打人群标签       |
| 11 月  | 259,377（浏览 244,994 / 加购 10,833 / 购买 3,550） | **验证**：检验 10 月打的标签能否预测 11 月购买 |

> **为什么拆两段？** 这是"时间外验证"（out-of-time validation）：所有模型参数只用 10 月拟合，11 月数据从头到尾不参与建模，只在最后当"考卷"。这样能防止**信息泄露**——比如用"11 月买了"去倒推 10 月应该给谁打高分，那分数就毫无意义了。

---

## 三、项目文件结构

```
project4.0/
├── main.ipynb      ← 唯一入口：数据分析全流程（含 EDA 图表），按顺序跑
├── analysis.py     ← 函数库：所有分析逻辑（特征、分层、检验、基准、打分）
├── config.py       ← 集中配置：路径、月份、随机种子、会话阈值等
├── tests/          ← 单元测试（13 个用例）
└── 产物：
    ├── validation_results.csv      ← 两项时间外验证结果
    └── tracked_users_list_Nov.csv  ← 733 名候选运营名单（10 列）
```

设计原则：**Notebook 只管"加载数据 + 画图 + 调用"，所有计算逻辑都在 `analysis.py` 里**，保证可复现、可测试。

---

## 四、流水线逐步骤详解（10 月建模）

### 步骤 1：数据质量检查

跑任何分析前先体检，只报告、不阻断。实测发现：

- ✅ 价格无缺失、无 ≤0 的购买；会话内无时间倒序
- ⚠️ **447 条完全重复行**（全部是 cart/view，不影响购买口径）→ 后续特征构建时自动去重
- ⚠️ brand 缺失 14.7%、category_code 缺失 32.1%
- ⚠️ **10 月未购用户中 48.4% 在 11 月完全没有任何活动**（这个后面验证解读要用）

### 步骤 2：特征工程（把事件日志变成"每个用户一行"）

对 10 月事件，按 `user_id + user_session` 排序，计算每个用户的 8 个原始特征：

| 特征                 | 怎么算                                 | 意义                                         |
| -------------------- | -------------------------------------- | -------------------------------------------- |
| `Pages_Viewed`       | 浏览事件数                             | 浏览深度                                     |
| `Estimated_Time`     | 会话内相邻事件间隔 < 30 分钟的累计时长 | 有效停留时长（避免跨会话空档被算进浏览时间） |
| `Session_Count`      | 不同会话数                             | 回访频率                                     |
| `Cart_Products`      | 加购事件的**去重商品数**               | 购物车意图                                   |
| `Purchased_Products` | 购买事件的去重商品数                   | 购买覆盖                                     |
| `Purchase_Frequency` | 购买事件数                             | 购买频次                                     |
| `Total_Spending`     | 购买金额之和                           | 消费贡献                                     |
| `Recency_Days`       | 观察期末距最近一次购买的天数           | 购买新近度（没买过的用户填一个统一的回填值） |

### 步骤 3：构造可解释指标（这是项目的"自定义部分"）

在原始特征之上，构建 3 类指标（`compute_engagement_metrics`）：

**① 探索度 `E_Score`**（衡量用户多"主动"）
```
E_Score = 平均( Z(log1p(浏览页数)), Z(log1p(停留时长)), Z(log1p(会话数)) )
```
其中 `log1p(x)=ln(1+x)` 压缩长尾（少数用户浏览几百页），`Z` 是标准化（转成均值 0、方差 1 的量纲），三者**等权平均**。

**② 摩擦分人群双口径**（衡量"买得有多费劲"，这是项目最有特色的设计）：

| 指标         | 定义                                             | 给谁用       | 语义                                                         |
| ------------ | ------------------------------------------------ | ------------ | ------------------------------------------------------------ |
| `Friction_0` | `max(去重加购商品数 − 去重购买商品数, 0)`，log1p | **未购用户** | "加购了却没买"——首购意图受阻的直接证据，与浏览页数无关       |
| `Friction_1` | `浏览页数 / (购买次数 + 1)`，log1p               | **已购用户** | 完成一次购买前的浏览成本；已购用户购买次数高，不会退化成纯浏览计数 |

> 为什么分两个？因为两种人群的"买不到"含义完全不同：未购用户要抓"有意图没转化"（加购未买），已购用户要抓"买东西很费劲"（浏览成本高）。混用同一个指标会互相污染。

**③ 价值指数 `Value_Index`**（已购用户的 RFM 连续化评分）
```
Value_Index = log1p(消费额) + log1p(购买频次) − log1p(最近购买天数)
```
传统 RFM 是"每维打 1-5 分再加权"，这里改成 log 变换后的等权相加（近度取负号：天数越大越差）。

### 步骤 4：用户分层（`segment_users`）——先分流，再各自切分

**为什么不直接对所有用户做 K-Means？** 因为 80% 的用户没买过，他们频次/金额全是 0，聚类会退化成一堆"零购买簇"、标签失去意义。所以采用**两阶段分层**：

```
              10月用户 (10,000)
                    │
        ┌────是否购买过────┐
        │ 未购 (8,018)     │ 已购 (1,982)
        ▼                 ▼
   E_Score 与 Friction_0    价值指数上四分位 (VIP)
   的 GMM 阈值               │
        │              ┌───┴───┐
  高潜力首购 539    高价值    常规已购 1,486
  普通浏览 7,479     496      (非 VIP)
                     │
              E_Score 与 Friction_1
              的 GMM 阈值
              ┌─────┼──────┐
          高摩擦 194  深度互动 102  直购 200
```

关键机制——**GMM 阈值（`gmm_intersection_threshold`）**：
- 对某个指标（如未购用户的 E_Score）拟合**双成分高斯混合模型**（假设人群由"低分群/高分群"两个正态分布混合而成）；
- 取两成分**后验概率相等**的位置作为切分阈值（比"两个均值的中点"更合理，因为它考虑了方差和权重）；
- 实测阈值：未购 E_Score = −0.257、未购 log(Friction_0) = 0.005、VIP E_Score = 1.047、VIP log(Friction_1) = 2.345、价值指数 = 5.339；
- **零膨胀保护**：92% 的未购用户加购数为 0，log(Friction_0) 是一堆 0 + 少量正数，GMM 交点退化成 ≈0，于是语义自动变成"**有加购即高摩擦**"——这恰好是期望的行为。

> 注意分层规则：高潜力首购 = 未购 **且** E_Score ≥ 阈值 **且** 有加购未买；高价值高摩擦 = VIP **且** 浏览成本 ≥ 阈值（此时不看 E_Score，摩擦优先）。

### 步骤 5：时间外验证（11 月检验，`rate_test`）

用 11 月"是否发生购买"作为结果变量，做两组对照：

**实验一：高潜力首购用户真的更可能首购吗？**
| 组别           | 人数  | 11 月购买率 | 95% CI           |
| -------------- | ----- | ----------- | ---------------- |
| 高潜力首购用户 | 539   | **17.44%**  | [14.47%, 20.87%] |
| 普通浏览用户   | 7,479 | **6.70%**   | [6.15%, 7.29%]   |

风险差 **+10.74 个百分点**，卡方检验 p = 8.75×10⁻²⁰（极显著）。→ 标签有效。

**实验二：高价值高摩擦用户是否在流失？**
| 组别                  | 人数 | 11 月购买率 | 95% CI           |
| --------------------- | ---- | ----------- | ---------------- |
| 高价值高摩擦用户      | 194  | **39.69%**  | [33.07%, 46.71%] |
| 同价值低摩擦 VIP 对照 | 302  | **53.97%**  | [48.34%, 59.51%] |

风险差 **−14.28 个百分点**，p = 0.00258（显著）。→ 高摩擦 VIP 确实更可能流失。

统计方法：卡方检验（Yates 校正）比较两组率；**Wilson 置信区间**（比朴素正态区间在小样本下更稳）；结果写入 `validation_results.csv`。

> ⚠️ 边界声明：这是**观察性时间外预测验证**，证明"标签能预测行为"，**不等于**"营销能带来增量"——真正的因果结论需要随机 A/B 实验。

---

## 五、升级层：逻辑回归基准（回答"规则到底有多好"）

分层规则是"手工阈值 + 二值标签"，它**解释性强但排序能力弱**。项目给两臂各跑了一个逻辑回归基准（5 折交叉验证，评估用折外 OOF 预测，避免自欺欺人）：

### 未购臂（首购预测，`nonbuyer_baseline`）

| 指标                  | 规则分层 | 逻辑回归   |
| --------------------- | -------- | ---------- |
| AUC（排序能力）       | 0.549    | **0.700**  |
| 同规模 Top-539 首购率 | 17.44%   | **22.45%** |
| Brier（校准误差）     | 0.118    | 0.066      |

AUC 差 bootstrap 95% CI (0.132, 0.169)——显著。**结论：规则是"保守的可解释代理"，个体排序能力明显弱于直接建模；同预算下 LR 圈人首购率从 17.4% 提到 22.4%。** 另外发现：LR + 规则标记 ≈ LR（规则不包含超出原始特征的信息）；加购特征在线性模型中边际贡献很小（因为加购用户本来就活跃，属于"活跃度"的中介）。

### 已购臂（复购预测，`buyer_baseline`）

| 指标                     | 数值                                             |
| ------------------------ | ------------------------------------------------ |
| 规则（高摩擦标记）AUC    | 0.525（≈随机）                                   |
| 手工价值指数 AUC         | 0.652                                            |
| **LR AUC (OOF)**         | **0.705**                                        |
| LR Top-194 复购率        | **68.56%**（规则组只有 39.69%）                  |
| **LR Bottom-194 复购率** | **12.89%**（真正的流失人群）                     |
| 学到的系数               | 频次 +0.59、近度 −0.38、页数 +0.33、金额 −0.15…… |

**结论**：① 高摩擦规则作为个体排序器同样接近随机，但它有可解释的人群差异（实验二 p=0.0026）；② **手工 RFM 价值指数（0.652）够用但不如学习式权重（0.705）**；③ 若要流失预警，LR 概率最低的 194 人（复购率 12.9%）比规则圈出的（39.7%）精准得多。

---

## 六、落地层：概率分 Top-k 圈人（`score_nonbuyers` / `score_buyers`）

分层规则保留作"解释层"（怎么触达、讲什么话术），**实际选人改用概率分排序（排序层）**：

- 未购用户 → `First_Purchase_Prob`（首购概率分）+ `First_Purchase_Rank` + `TopK_Flag`（同预算下应触达的前 k 人）
- 已购用户 → `Repurchase_Prob`（复购概率分）+ `Repurchase_Rank`（Top-k 复购运营 / Bottom-k 流失预警）

名单 `tracked_users_list_Nov.csv`（733 人：539 高潜力首购 + 194 高价值高摩擦，10 列）：

```
user_id, User_Segment, E_Score, Friction, Value_Index,
First_Purchase_Prob, First_Purchase_Rank, TopK_Flag,
Repurchase_Prob, Repurchase_Rank
```

运营用法：按预算取 `rank ≤ 预算` 即可圈人；高潜力走"购物车挽回 + 首购券"，高摩擦 VIP 走"体验排障"。

---

## 七、如何运行

1. `pip install -r requirements.txt`（版本已锁定）；
2. 打开 `main.ipynb` 从上到下运行（每个 cell 有注释说明）：
   数据加载 → EDA（分布/相关/加购论证）→ 特征构建 → 分层概览 → 结构透视 → 实验一/二验证 → 未购臂基准 → 已购臂基准 → 概率分圈人 → 名单导出 → 敏感性分析；
3. 产物自动生成 `validation_results.csv` 与 `tracked_users_list_Nov.csv`；
4. `python -m unittest discover -s tests` 跑 13 个单元测试。

---

## 八、一张图总结全流程

```
10月事件日志(308,850条)
   │ 去重 + 数据质量检查
   ▼
特征工程(会话时长/聚合) ──► 指标构建(E_Score, Friction_0/1, Value_Index)
   │                                   │
   │                    GMM 阈值分层（未购/已购两阶段）
   │                                   │
   ▼                                   ▼
用户特征表(10,000×10)          六类人群标签（高潜力首购 539 / 高摩擦 VIP 194 / …）
   │                                   │
   └──────────────┬────────────────────┘
                  ▼
         11月时间外验证（χ²+Wilson CI，两实验均显著）
                  │
                  ▼
         LR 基准（未购臂 AUC 0.700 / 已购臂 AUC 0.705）
                  │
                  ▼
         概率分 Top-k 圈人（首购分 + 复购分）
                  │
                  ▼
    tracked_users_list_Nov.csv（733 人，10 列）→ 随机 A/B 实验抽样框
```

**一句话版本**：用 10 月行为把用户分成"未购（看首购潜力）"和"已购（看价值与复购）"两翼，各自用可解释指标 + GMM 阈值打标签，用 11 月独立数据验证标签有效，再用逻辑回归证明"直接建模排序更强"，最终输出带概率分的运营名单供 A/B 实验使用。





## 逻辑回归基准和概率分 Top-k 圈人这两部分

先用一句话把这两块说清楚（它们其实各管一件事）

| 部分                    | 一句话                                                       | 类比                                            |
| ----------------------- | ------------------------------------------------------------ | ----------------------------------------------- |
| 规则分层                | 给用户**贴标签**：你是"高潜力首购"还是"普通浏览"             | 按期末成绩**分班**（优等班/普通班），班名好懂   |
| LR 概率分 Top-k         | 给用户**打分数、排序**：先触达分数高的前 N 个                | 每次考试按分数**排名取前 N 名**，更准但不给班名 |
| LR 基准（你问的另一块） | **证明**"排名比分班更能预测下个月买不买"（AUC 0.549 vs 0.700） | 质检报告：分班和排名，哪个更灵验                |

所以整个项目的选人逻辑是：**先做了分班（规则）→ 质检发现排名更准（基准）→ 于是选人改用排名（概率分），但保留分班用来讲策略话术**。你感觉复杂，是因为这三层叠在一起；其实拆开就一句话：**标签用于解释，概率分用于选人，基准用于说服"为什么要用概率分选人"。**